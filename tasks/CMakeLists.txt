# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(gazebo_usvTaskLib)
ADD_LIBRARY(${GAZEBO_USV_TASKLIB_NAME} SHARED
    ${GAZEBO_USV_TASKLIB_SOURCES})
add_dependencies(${GAZEBO_USV_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${GAZEBO_USV_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${GAZEBO_USV_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GAZEBO_USV_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GAZEBO_USV_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${GAZEBO_USV_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${GAZEBO_USV_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GAZEBO_USV_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GAZEBO_USV_TASKLIB_HEADERS}
    DESTINATION include/orocos/gazebo_usv)

